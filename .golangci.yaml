version: "2"
linters:
    default: standard
issues:
    fix: true
formatters:
    enable:
        - gci
        - gofmt
        - gofumpt
        - goimports
        - golines
run:
    timeout: 5m
    allow-parallel-runners: true
    go: '1.25'
