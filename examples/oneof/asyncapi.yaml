asyncapi: 3.1.0
info:
  title: Orders Service
  version: 1.0.0
  description: Order lifecycle events
  contact:
    name: Orders Team
    email: orders@example.com
servers:
  prod:
    host: broker.example.com:9092
    protocol: kafka
    description: Production Kafka cluster
defaultContentType: application/json
channels:
  order-event:
    address: order-event
    messages:
      OrderEvent:
        payload:
          $ref: "#/components/schemas/github.com~1RubenRibGarcia~1asyncgo~1examples~1oneof.OrderEvent"
        name: OrderEvent
    description: Envelope carrying exactly one of the order event types
    bindings:
      kafka:
        partitions: 3
        topic: order-events
operations:
  order-event.send:
    action: send
    channel:
      $ref: "#/channels/order-event"
    messages:
    - $ref: "#/channels/order-event/messages/OrderEvent"
components:
  schemas:
    github.com/RubenRibGarcia/asyncgo/examples/oneof.OrderCancelled:
      type: object
      properties:
        order_id:
          type: string
        reason:
          type: string
          description: Reason for the cancellation
      required:
      - order_id
    github.com/RubenRibGarcia/asyncgo/examples/oneof.OrderEvent:
      type: object
      properties:
        data:
          oneOf:
          - $ref: "#/components/schemas/github.com~1RubenRibGarcia~1asyncgo~1examples~1oneof.OrderPlaced"
          - $ref: "#/components/schemas/github.com~1RubenRibGarcia~1asyncgo~1examples~1oneof.OrderCancelled"
      required:
      - data
    github.com/RubenRibGarcia/asyncgo/examples/oneof.OrderPlaced:
      type: object
      properties:
        amount:
          type: number
          format: double
        note:
          type: string
          description: Optional note from the customer
        order_id:
          type: string
      required:
      - order_id
      - amount
