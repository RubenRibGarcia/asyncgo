---
# Hand-authored fragment merged over the generated document (see spec.Overlay).
# Non-zero values here override the Go-derived document; anything omitted is
# preserved. This demonstrates sub-decision #4: topology can be authored in Go
# OR in YAML.
info:
  license:
    name: MIT

channels:
  order-cancelled:
    address: order-cancelled
    description: Emitted when an order is cancelled
